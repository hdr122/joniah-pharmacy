{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm install && pnpm run build"
  },
  "deploy": {
    "startCommand": "pnpm run db:push && pnpm start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
